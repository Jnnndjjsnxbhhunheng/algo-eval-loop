---
name: algo-eval-loop
description: >
  算法-产品评估迭代工作流 Skill。管理算法产出结果的评估、反馈分析、代码迭代的完整闭环。
  支持自动迭代模式（借鉴 karpathy/autoresearch）：修改算法 → 运行 → 自动评估 → 保留/回滚 → 循环。
  当用户提到以下场景时触发：评估算法结果、分析产品反馈、迭代算法代码、对比版本差异、
  生成评估报告、把算法输出转成Excel、查看或更新评估标准、跑一版新结果、根据反馈改代码、
  版本对比、badcase分析、产品给了评估表要分析、自动跑实验、自动迭代、过夜跑优化。
  典型触发语句："帮我看看产品的评估结果"、"这一轮反馈有什么问题"、"改完代码跑一下"、
  "把结果转成Excel给产品"、"对比v1和v2的差异"、"评估标准要加一个维度"、
  "产品给了一份评估Excel"、"这个markdown结果帮我整理成产品要的格式"、
  "帮我分析一下这份反馈"、"算法跑出来了帮我转一下"、
  "自动跑几轮看看"、"过夜跑优化"、"自动迭代到收敛"。
---

# 算法-产品评估迭代工作流

## 概览

本 Skill 管理算法与产品之间的评估迭代闭环。核心循环是：

```
算法产出结果 → 转为产品可评估的Excel → 产品按标准评估 → 分析反馈 → 迭代算法 → ...
```

### 两种工作模式

**手动模式**（默认）：用户在任意节点进入，Claude 判断并执行对应操作。
- 算法侧："跑出来了，帮我转成Excel"
- 产品侧："产品给了评估结果，帮我分析"
- 标准侧："评估维度要改一下"
- 迭代侧："上次的问题，帮我改代码跑一版"

**自动迭代模式**（借鉴 [karpathy/autoresearch](https://github.com/karpathy/autoresearch)）：无需产品介入，Claude 自主完成「修改 → 运行 → 评估 → 保留/回滚」的完整闭环，持续优化直到收敛或达到轮次上限。
- "自动跑几轮看看"
- "过夜跑优化"
- "自动迭代到收敛"

```
┌─────────────────────────────────────────────────────────────┐
│  自动迭代循环（autoresearch 理念）                             │
│                                                             │
│  修改算法代码 → git commit → 运行算法 → 自动评估指标            │
│       ↑              │                       │              │
│       │              │            ┌──────────┴──────────┐   │
│       │              │            ▼                     ▼   │
│       │              │        指标改善              指标退化  │
│       │              │        保留提交              git回滚   │
│       │              │            │                     │   │
│       └──────────────┴────────────┴─────────────────────┘   │
│                                                             │
│  记录到 results.tsv → 继续下一轮 / 达到停止条件时退出           │
└─────────────────────────────────────────────────────────────┘
```

Claude 的任务是判断用户当前处于哪个节点，执行对应操作，并在合适时机引导进入下一步。

## 工作目录结构

首次触发任何操作时，检查 `eval-workspace/` 是否存在。不存在则创建：

```
eval-workspace/
├── eval-criteria.md          # 评估标准文档（可迭代更新）
├── versions/
│   ├── v1/
│   │   ├── raw_output/       # 算法原始输出（保留原格式，不限类型）
│   │   ├── output.xlsx       # 转换后给产品的 Excel
│   │   ├── feedback.xlsx     # 产品评估反馈（产品填完后放回来）
│   │   ├── analysis.md       # 反馈分析报告
│   │   └── plan.md           # 迭代改进方案
│   ├── v2/
│   │   └── ...
│   └── changelog.md          # 版本变更日志
├── autoloop/                 # 自动迭代循环产物（autoresearch 模式）
│   └── {timestamp}/
│       ├── results.tsv       # 每轮实验记录
│       └── autoloop_summary.md  # 循环结束摘要报告
└── reports/
    └── comparison_v1_v2.md   # 跨版本对比报告
```

## 入口判断

根据用户的输入判断当前要做什么。以下是常见入口和对应操作：

| 用户输入特征 | 对应操作 |
|-------------|---------|
| 提供了算法输出文件（任意格式）或说"结果出来了" | → 格式转换 |
| 提供了产品填写的评估Excel或说"反馈回来了" | → 反馈分析 |
| 说"怎么改"、"生成改进方案" | → 迭代方案 |
| 说"改吧"、"跑一下"、确认了方案 | → 执行迭代 |
| 说"对比版本"、"v1和v2差异" | → 版本对比 |
| 说"加个评估维度"、"改评估标准" | → 标准管理 |
| 首次使用或说"新建项目" | → 初始化 |
| 说"自动跑"、"过夜优化"、"自动迭代" | → **自动迭代循环** |

不确定时直接问用户想做什么，不要猜。

## 操作：格式转换（算法输出 → 产品Excel）

这是最灵活的环节。算法输出可能是任何格式——JSON、Markdown表格、CSV、纯文本、Python打印的日志、甚至一段粘贴到对话里的内容。核心目标不变：**理解内容，按业务目标转成产品能评估的Excel**。

**执行步骤：**

1. **读取并理解**算法输出内容
   - 判断格式（JSON / Markdown / CSV / 文本 / 其他）
   - 理解数据结构：哪些是数据字段，哪些是元信息
   - 如果结构不清楚，问用户："这份输出中，哪些是需要产品评估的主要数据项？"

2. **确定转换目标**
   - 如果 `eval-criteria.md` 存在，读取评估维度
   - 如果不存在，问用户评估维度有哪些，然后同步创建 `eval-criteria.md`
   - 确认产品需要的Excel里应该包含哪些列（数据列 + 评估列）

3. **自主执行转换**
   - 根据对数据结构的理解，自行编写 Python 代码完成转换（openpyxl / pandas 均可）
   - 不要套用固定模板，每次根据实际数据结构决定怎么处理：
     - 扁平结构 → 直接映射为 Excel 行列
     - 嵌套结构 → 判断展平还是提取关键字段，以产品评估便利性为准
     - Markdown 表格 → 解析表格结构
     - 非结构化文本 → 提取关键信息，按业务逻辑组织成表
   - 列名使用业务语言（如 `pred_score` → "预测得分"），不确定时问用户
   - 追加评估列：每个维度生成 `[维度名]_评分` 和 `[维度名]_备注` 两列
   - 格式上注意：表头加粗、评估列用醒目底色标记、冻结首行、合理列宽

4. **保存到版本目录**
   - 确定版本号（`versions/` 下最新版本 +1）
   - 原始输出放 `raw_output/`（保留原始格式不动）
   - 转换后的 Excel 放 `output.xlsx`

5. **通知用户**
   - 告知版本号和 Excel 路径
   - 提醒产品填写评分列和备注列后返回

**关键原则**：转换不是机械的格式搬运。产品看到的 Excel 应该像是一个人为他们整理好的评估表——列名清晰、数据排列合理、评估列一目了然。如果原始数据有嵌套结构，需要判断是展平还是保留关键字段。做这个判断时优先考虑产品评估的便利性。

## 操作：反馈分析

**触发**：用户提供了产品填写的评估 Excel，或指向某个版本的 `feedback.xlsx`。

这也是一个常见的启动入口——产品把评估完的 Excel 给算法同学，算法同学拿过来就想看分析。

**执行步骤：**

1. 读取评估 Excel
   - 识别数据列和评估列（评估列通常带 `_评分`、`_备注` 后缀，但也可能是产品自己加的列名）
   - 如果列名不标准，问用户哪些是评估列
   - 保存到对应版本目录的 `feedback.xlsx`（如果还没有对应版本目录，创建一个）

2. **统计分析**
   - 自行编写 Python 代码读取 Excel 并分析（pandas / openpyxl）
   - 核心指标：各维度均分、分布、低分占比、bad case 数量

3. **Bad case pattern 分析**（这是最有价值的部分）
   - 不要逐条罗列 bad case，要找共性
   - 常见聚类角度：输入类型、数据来源、数值区间、文本长度、特殊字符
   - 输出格式："X% 的低分 case 具有 [某某共性]，集中在 [某维度]"
   - 如果有上一版本的反馈，做跨版本对比：改善了什么、退化了什么

4. **生成 `analysis.md`** 写入版本目录

5. **向用户呈现摘要**
   - 总体得分概况（一两句话）
   - Top 3-5 个问题 pattern
   - 退化项（如果有上一版本）
   - 自然地引导："要不要看看改进方案？"

## 操作：生成迭代方案

**触发**：分析完反馈后自然进入，或用户直接要求。

1. 基于 `analysis.md` 中的问题 pattern
2. 阅读算法代码，理解当前实现逻辑
3. 生成 `plan.md`：
   - **问题归因**：每个 pattern 对应代码哪部分
   - **改进措施**：具体改什么、怎么改（要具体到函数/逻辑层面）
   - **预期效果**：改完后哪些 case 应该改善
   - **风险评估**：可能影响的已有好结果
4. 展示方案，等用户确认或调整

## 操作：执行迭代 → 验证

**触发**：用户确认了方案。

1. **备份**当前算法代码
2. **修改代码**，按 `plan.md` 中的措施逐项改
   - 每处改动加注释说明原因
   - 每次只改一个方向的问题，不要大范围重构
3. **运行算法脚本**获得新输出
4. **快速验证**：
   - 拿上一版的 bad cases 跑新版本，检查是否改善
   - 检查是否有退化
5. 验证通过 → 走格式转换，准备新一轮产品评估
6. 验证有问题 → 告知用户，建议调整方案

## 操作：版本对比

读取两版反馈 Excel，自行编写分析代码进行对比：
- 各维度得分变化
- 逐条匹配对比（改善和退化的具体 case）
- 输出到 `reports/comparison_vX_vY.md`

## 操作：评估标准管理

评估标准文档 `eval-criteria.md` 是整个工作流的驱动文件。格式规范参见 `references/criteria-guide.md`。

**读取时机**：格式转换（生成评估列）、反馈分析（按维度统计）、迭代方案（关联标准与代码）

**更新操作**：
1. 读取当前 `eval-criteria.md`
2. 应用修改（新增/删除/调整维度）
3. 在变更记录中注明原因和生效版本
4. 提醒用户：新标准从下一轮评估生效，已有版本的评估不受影响

## 操作：自动迭代循环（Auto-Loop）

借鉴 [karpathy/autoresearch](https://github.com/karpathy/autoresearch) 的理念——让 AI agent 自主执行「修改代码 → 运行实验 → 评估指标 → 保留或回滚」的循环，无需人工守在旁边。

**触发**：用户说"自动跑几轮"、"过夜跑优化"、"自动迭代到收敛"，或明确希望无人值守式优化。

**前提条件**（启动前必须确认）：
1. **算法脚本路径**：可运行的入口脚本（如 `train.py`、`run.py`）
2. **评估指标**：用什么指标衡量好坏（如准确率、F1、loss、自定义得分），以及方向（越高越好 / 越低越好）
3. **时间预算**：每轮实验的最大运行时间（默认 5 分钟，参考 autoresearch 的固定时间窗口）
4. **轮次上限**：最多跑多少轮（默认 20 轮）
5. **收敛条件**：连续 N 轮无改善则停止（默认 5 轮）

不确定任何一项时，问用户。

**执行流程**：

```
初始化：
  1. git checkout -b autoloop/{timestamp}   # 创建实验分支
  2. 运行基线版本，记录基线指标
  3. 初始化 results.tsv 实验记录表

循环（每一轮）：
  1. 【分析】读取上一轮结果和历史趋势，决定本轮改什么
     - 首轮：基于 eval-criteria.md 和代码理解选择优化方向
     - 后续：基于 bad case pattern + 历史实验趋势选择
  2. 【修改】修改算法代码（只改一个方向，小步迭代）
  3. 【提交】git commit -m "experiment {N}: {改动描述}"
  4. 【运行】执行算法脚本，设置超时保护
  5. 【评估】自动评估输出结果（见下方自动评估策略）
  6. 【决策】
     - 指标改善 → 保留 commit，记录为 ✅ advance
     - 指标持平 → 保留 commit，记录为 ➡️ neutral
     - 指标退化 → git reset --hard HEAD~1，记录为 ❌ revert
     - 运行崩溃 → git reset --hard HEAD~1，记录为 💥 crash，分析原因
  7. 【记录】写入 results.tsv 和 changelog
  8. 【判断停止条件】
     - 达到轮次上限 → 停止
     - 连续 N 轮无改善 → 停止（收敛）
     - 指标达到目标阈值 → 停止（达标）
```

**results.tsv 格式**（每轮一行，追加写入）：

```
轮次	时间戳	改动描述	指标名	指标值	基线值	变化	决策	commit_hash
1	2025-01-15T10:00	调整权重衰减参数	val_loss	0.432	0.450	-0.018	advance	a1b2c3d
2	2025-01-15T10:06	增大batch_size	val_loss	0.445	0.432	+0.013	revert	-
3	2025-01-15T10:12	替换激活函数为GELU	val_loss	0.428	0.432	-0.004	advance	e4f5g6h
```

### 自动评估策略

自动迭代不等产品反馈，需要自动评估。按以下优先级选择评估方式：

1. **定量指标**（最优先）：如果算法脚本会输出数值指标（loss、accuracy、F1 等），直接解析脚本输出或结果文件
2. **测试集自动评分**：如果有标注好的测试集（ground truth），自动计算准确率等指标
3. **规则评估**：对算法输出应用 eval-criteria.md 中定义的规则进行自动打分
4. **LLM 辅助评估**：用 LLM 对输出结果按评估标准打分（适用于文本生成、推荐等主观维度）

评估方式在启动时确认，运行过程中保持不变以确保可比性。

### 安全机制

- **超时保护**：每轮运行超过时间预算自动终止
- **Git 隔离**：所有实验在独立分支，不影响主分支
- **回滚能力**：每次修改前 commit，退化时自动 `git reset`
- **资源监控**：（可选）监控 CPU/GPU/内存使用，异常时暂停
- **中断恢复**：results.tsv 持久化，中断后可从上次状态继续
- **最终报告**：循环结束后生成 `autoloop_summary.md`，包含所有实验结果、最佳版本、改善历程

### 自动迭代 vs 手动迭代

| 维度 | 手动模式 | 自动迭代模式 |
|------|---------|------------|
| 评估方 | 产品手工打分 | 自动指标/LLM评估 |
| 人工介入 | 每步需确认 | 仅启动和结束 |
| 迭代速度 | 一天1-2轮 | 一小时10+轮 |
| 适用场景 | 主观评估、需要产品视角 | 有明确量化指标的优化 |
| 风险控制 | 人工把关 | Git回滚 + 超时保护 |

两种模式可以混合使用：先自动跑 N 轮找到较优版本，再转给产品做主观评估。

## 关键原则

**格式转换是理解而非搬运**：不管输入是什么格式，产出的 Excel 要像是一个懂业务的人整理的——列名清晰、结构合理、评估列一目了然。遇到不确定的结构，问用户而非猜测。

**分析找 pattern 而非罗列**：几百条 bad case 逐条列出毫无意义。聚类、找共性、给可操作的结论。

**代码改动保守**：每次聚焦一个方向，小步迭代。改完必须验证，防止退化。

**版本可追溯**：每个版本的所有产物（原始输出、Excel、反馈、分析、方案）都保留。

**评估标准是活的**：随业务演化，每次变更记录在案。

**不要假设入口**：用户可能从任何节点进入，判断不了就问。

**自动迭代要大胆尝试但安全回滚**：每次只改一个方向，commit 后再跑。改善就保留，退化就回滚。不怕试错，怕的是无法回滚。这是 autoresearch 的核心哲学：让实验的成本趋近于零。
